
# OpenSchema TypeScript Validator & CLI (CommonJS build)

This version targets **Node.js (CommonJS)** to avoid ESM headaches (.js extensions, import attributes).
It compiles cleanly under **TypeScript 5.6** with **strict** mode, **Ajv 8**, and **Node types**.

## Quick start
```bash
npm install
npm run build
npm run validate
```
