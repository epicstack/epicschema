{
  "name": "epicschema",
  "private": true,
  "packageManager": "pnpm@9.12.1",
  "workspaces": [
    "packages/*",
    "runner"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "conformance": "pnpm --filter runner test:conformance"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "typescript": "^5.6.3"
  }
}